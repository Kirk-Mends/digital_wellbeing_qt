name: Build and Upload to App Store
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: false

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install PySide6 pyinstaller psutil
          if [ -f req.txt ]; then pip install -r req.txt; fi

      - name: Build App
        run: |
          # We add the bundle-identifier here
          pyinstaller --noconfirm --windowed \
          --name "DigitalWellbeing" \
          --osx-bundle-identifier "com.kirkmends.digitalwellbeing" \
          --icon "assets/icons/icon.icns" \
          --add-data "assets:assets" \
          --add-data "core:core" \
          --add-data "ui:ui" \
          main.py
          
          # This adds the version numbers Apple's error was complaining about
          /usr/libexec/PlistBuddy -c "Add :CFBundleVersion string 1.0.0" "dist/DigitalWellbeing.app/Contents/Info.plist"
          /usr/libexec/PlistBuddy -c "Add :CFBundleShortVersionString string 1.0.0" "dist/DigitalWellbeing.app/Contents/Info.plist"

      - name: Upload to App Store Connect
        env:
          APPLE_ID: "kirkmendsa@gmail.com"
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
        run: |
          # Re-zip it now that the version info is inside
          ditto -c -k --sequesterRsrc --keepParent "dist/DigitalWellbeing.app" "dist/DigitalWellbeing.zip"
          
          xcrun altool --upload-app \
            --type osx \
            --file "dist/DigitalWellbeing.zip" \
            --username "$APPLE_ID" \
            --password "$APPLE_PASSWORD"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Mac-App-Build
          path: dist/*.app